name: CI Pipeline

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run CI
        run: echo "CI running on ${{ github.ref }} at ${{ github.sha }}"
